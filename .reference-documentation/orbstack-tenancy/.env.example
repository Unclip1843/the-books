# ===== Local defaults =====
# Copy this file to .env and adjust values as needed.

# Supervisor + tenant URLs
PUBLIC_BASE_URL=http://localhost:8080
COOKIE_DOMAIN=localhost

# Secrets (replace before sharing).
# Fetch current values from the shared secrets bundle (ask ops if you need access).
COOKIE_JWT_SECRET=replace-with-shared-secret
REDIS_PASSWORD=replace-with-shared-secret
TENANT_NAMESPACE_KEY=replace-with-shared-secret

# Runtime tuning
CENTRAL_DB_PATH=/var/lib/supervisor/central.db
IDLE_MINUTES=20
WARMUP_HEALTH_TIMEOUT_SECONDS=45
TENANT_IMAGE=tenant-base:latest
TENANT_EXPOSE_PORT=8080
TENANT_CPU=2
TENANT_RAM_GB=4
TENANT_PIDS=512

# API keys (optional for local dev)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# ===== Production overrides =====
# Uncomment and set these when deploying behind Cloudflare
# PUBLIC_BASE_URL=https://backend.yourdomain.com
# COOKIE_DOMAIN=backend.yourdomain.com
# TUNNEL_TOKEN=your-cloudflare-tunnel-token
